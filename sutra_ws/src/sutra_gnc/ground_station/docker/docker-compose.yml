version: '3.8'

services:
  sutra-gcs:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: sutra_gcs_production
    ports:
      - "80:80"
      - "14540:14540/udp"
    restart: always
    environment:
      - NODE_ENV=production
      - MAVLINK_UDP_PORT=14540
