apiVersion: v1
kind: Service
metadata:
  name: smart-horizon-gcs-service
  namespace: sutra-uav-system
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: smart-horizon-gcs
