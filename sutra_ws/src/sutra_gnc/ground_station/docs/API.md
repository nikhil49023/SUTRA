# SUTRA GCS API Reference

## Subsystem Services
* `MissionEngine.startMission(waypoints)`
* `ConnectionManager.connectVehicle(sysId, url, type)`
* `SwarmManager.getNodes()`
* `TaskAllocator.allocateTask(taskId, lat, lng)`
* `ThreatAssessmentEngine.evaluateThreats(telemetry, targets)`
* `RTSPManager.getStreams()`
